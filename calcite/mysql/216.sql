SELECT t2.DEPTNO, SUM( MIN(t2.COMM) ), MIN( SUM(DISTINCT t2.SAL) ), SUM( SUM( MIN(t2.COMM) ) ) FROM ( SELECT EMP0.DEPTNO, EMP0.SAL, SUM(EMP0.COMM), MIN(EMP0.COMM) AS COMM FROM EMP AS EMP0 GROUP BY EMP0.DEPTNO, EMP0.SAL ) AS t2 GROUP BY t2.DEPTNO
