SELECT t2.EMPNO, SUM(AVG(DISTINCT DEPTNO0)), AVG(SUM(AVG(DISTINCT DEPTNO0))) FROM (SELECT EMP0.EMPNO, DEPT0.DEPTNO AS DEPTNO0, COUNT(*) AS CNT FROM EMP AS EMP0 INNER JOIN DEPT AS DEPT0 ON EMP0.DEPTNO = DEPT0.DEPTNO GROUP BY EMP0.EMPNO, DEPT0.DEPTNO) AS t2 GROUP BY t2.EMPNO
