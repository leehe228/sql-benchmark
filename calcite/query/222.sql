SELECT t6.ENAME, MIN( MIN(t6.EMPNO) ) FROM ( SELECT EMP1.ENAME, MIN(EMP1.EMPNO) AS EMPNO FROM EMP AS EMP1 GROUP BY EMP1.ENAME UNION ALL SELECT EMP2.ENAME, MIN(EMP2.EMPNO) AS EMPNO FROM EMP AS EMP2 GROUP BY EMP2.ENAME ) AS t6 GROUP BY t6.ENAME